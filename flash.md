学习来源：
0.05 复制打开抖音，看看【谦行AIing的作品】栈：只在尾部操作的 List # 栈 # stac... https://v.douyin.com/08usizS0StQ/ 08/01 JiP:/ h@O.kp :5pm

无事
有点忘记了数据结构
巩固一下数据结构
以及加深理解
关联此前所学的其他内容知识

对于数组 数组是连续的内存存储块 意味着我们很方便就能通过索引去查找数据 也就是可以随机访问（addr(i)=base+i*size 而之所以下标要从0开始也是为了计算机cpu考虑 cpu只需要进行一次加法和乘法就能算出任意元素的内存地址） 但是由于特性 内存连续 如果我想要删除某个元素或者插入某个元素 需要移动大量的元素 而且可能导致空间不足 并且操作是o(n)时间复杂度 意味着这是数组极大的缺点 以及在空间效率上数组固定大小 需要扩容
为了解决数组固定大小的问题 诞生了动态数组 可以自动扩容 但是代价也是沉重的 需要申请内存 数据需要复制搬运 o(n)的时间复杂度 于此同时扩容倍数太大 浪费内存 扩容太小 需要频繁扩容 浪费性能 频繁卡顿   

但是链表 离散存储 逻辑连续 截然相反 链表可以跨内存区域存在 不一定需要连续的存储 那么也意味着链表的插入和删除将变得极为方便几乎就是o(1)的时间复杂度 而链表没有索引的概念 也意味着链表几乎纯靠递归去查找和读取第几个元素 时间复杂度是o(n)  在空间效率上链表是动态申请内存
链表一般末尾指向NULL 如果末尾指向头节点 则是循环列表 而如果节点有头指针和尾指针 变成双向链表 则增加了内存占用

栈先进后出
对于栈有什么用处呢 典型应用就是 编译器的括号匹配原理 编译器如何知道你写的{[()]}是否闭合 
规则：遇到左括号->入栈（等待匹配）
遇到右括号->检查栈顶 匹配？出栈（消消乐） 不匹配 报错
还有应用就是后缀表达式：
计算机不喜欢括号和优先级 只喜欢后缀表达式
对于（中缀）人而已比如3+5*2 
如果这只是字符 那么计算机怎么只知道优先级和顺序呢（如果是数字计算机自然会计算）
（后缀）计算机：3 5 2 * +
后缀求值算法：
遇到数字入栈 遇到符号出栈 弹出两个数的计算结果 结果入栈
2    
5                   10
3                   3               13
1.入栈3,5,2   2.遇到* ，5*2=10   3.遇到＋，3+10=13
当然栈还有很多应用
凡是符号“最后做的事情要先被撤回/先被处理”或“先走到底，再一层层往回退”的问题，本质上都适合用栈解决
深度优先搜素（DFS）
利用栈“先走到最深，再按照相反的顺序回退”，逐层展开并回退
回溯算法：
保存决策路径，失败时快速撤销最近的一次选择
递归显示实现：
手动保存函数状态，模拟系统调用栈
系统以及工程应用：
OS函数调用栈：
保证函数遵循后调用，先返回的顺序执行
浏览器的前进和后退：
通过栈记录访问历史，实现逆序回退
撤销操作：
按照相反的顺序回放，优先撤销最近的修改
总之 基于动态数组的话去实现栈 既可以动态扩容还能实现 而且简单高效 如果用简单数组则容量固定 用链表则有额外节点开支